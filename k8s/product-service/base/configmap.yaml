apiVersion: v1
kind: ConfigMap
metadata:
  name: product-service-config
data:
  NODE_ENV: 'production'
  PORT: '3001'

  # DB connectivity (service name inside the cluster)
  DB_HOST: 'postgres'
  DB_PORT: '5432'
  DB_NAME: 'ecommerce'